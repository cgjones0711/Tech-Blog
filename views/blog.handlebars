<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="laptop">💻</span>
    <span class="form input">{{format add_comment}</span>
    
  </p>
  <p>
    <button class="btn btn-primary">Add a Comment!</button>
  </p>
</div>
